**Full CMS Architecture Cleanup & Feature Integration**

Hello,

Please perform a **complete architectural cleanup and refactor of the Laravel project** to ensure the system behaves like a professional CMS + Client Portal platform.

Currently several parts of the system appear inconsistent or partially implemented.  
The goal is to create a **clean, secure, and maintainable architecture**.

---

# 1. Security Fix (Critical)

✅ **Done** — New users are no longer granted admin access.

New users registering via `/signup` must **NOT receive admin access**.

Requirements:

- [x] New users must be created with role = `client`
- [x] Only users with role = `admin` may access `/admin` (Filament panel)
- [x] Clients must be redirected to `/dashboard`
- [x] Admins must be redirected to `/admin`

Implement proper authorization middleware.

---

# 2. Model & Database Cleanup

Audit and clean all models.

Unify and simplify the structure.

Recommended models:

Users  
Clients  
Projects  
Services  
Features  
Orders  
Testimonials  
Publications (Blog)  
Guides  
Pages  
SiteSettings

Remove duplicates such as:

PortfolioProjects  
Projects

Use **Projects as the single model** and display it on frontend as **Portfolio**.

Add fields:

technologies  
project_url  
images  
client_id

---

# 3. Testimonials Improvement

Currently testimonials require manually entering the client name.

Instead:

Testimonial must have a relationship to Client.

Example:

Testimonial  
client_id  
rating  
comment

Filament form must use a **client dropdown** instead of text input.

---

# 4. Services Improvement

Service cards must support:

name  
description  
icon  
image  
base_price

Frontend must display them in the same layout as the original React design.

Clicking a service should allow starting an order with that service pre-selected.

---

# 5. Orders System Cleanup

Current order options appear duplicated or inconsistent.

Create structured models for order configuration.

Example:

OrderTypes  
AdditionalFeatures  
BudgetRanges  
TimelineOptions

These must be editable in the admin panel.

Remove duplicated services/features in the order form.

---

# 6. Pages System (CMS)

About and Contact pages must be **CMS-driven**, not static blade pages.

Create Page entries:

slug = about  
slug = contact

Fields:

title  
content  
seo_title  
seo_description

Contact page must support:

address  
phone  
email  
map_embed

Editable via Filament.

---

# 7. Site Settings

SiteSettings must store global configuration:

site_name  
site_email  
site_phone  
address  
footer_text  
google_maps_embed  
social_links

Admin panel must allow editing these values.

---

# 8. Client Dashboard

Client dashboard must show:

My Projects  
My Orders  
Messages  
Profile

Add project status indicators and order history.

---

# 9. Domain Search Feature

The original design included a **domain search tool**.

Implement a domain availability checker.

Possible approach:

Use a domain API or WHOIS lookup.

Example workflow:

User enters domain →  
System checks availability →  
Display Available / Taken result.

This can be a frontend tool without requiring registration.

---

# 10. Pricing Calculator

The original design included a **service price calculator**.

Create a dynamic calculator where users can:

Select services  
Select additional features  
See total price update dynamically.

Frontend must calculate totals using JavaScript.

Orders must save selected options.

---

# 11. Frontend Navigation Fix

Some pages show inconsistent menus.

Ensure all pages use the same layout:

Home  
Services  
Portfolio  
Blog  
Guides  
About  
Contact  
Order

Menu alignment must remain consistent across pages.

---

# 12. DDEV Environment

Do NOT run:

php artisan serve

The project runs in **DDEV**.

Use the existing environment:

[https://archvadze.ddev.site:33001](https://archvadze.ddev.site:33001/)

---

# 13. Final Deliverables

After completion provide:

1. List of models and relationships

2. Updated database schema

3. Admin panel structure

4. Screenshots or explanation of frontend pages

5. Confirmation that all pages render correctly

The goal is a **clean, professional Laravel CMS architecture** where:

Admin Panel manages content  
Frontend displays dynamic content  
Client Dashboard manages customer activity
